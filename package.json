{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "src/core",
    "src/infrastructure/storage",
    "src/presenter/express",
    "src/presenter/graphql"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npm run clean && tsc -b ./tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
